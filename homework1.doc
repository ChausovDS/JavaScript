var a = 1, b = 1, c, d;
c = ++a; alert(c);           // 2 

Инкремент а: а становится равно 2(1+1), возвращается значение 2

d = b++; alert(d);           // 1

Инкремент b: b становится равно 2, но так как инкремент постфиксный, то 
возвращается старое значение b = 1

c = (2+ ++a); alert(c);      // 5

2 + инкремент а: а, после инкремента равное 2, становится на 1 больше (3),
складывается с 2 и в сумме будет 5 (2+3)

d = (2+ b++); alert(d);      // 4

2 + инкремент b: то же самое, но инкремент постфиксный. Поэтому, хотя значение b и равно 3, но вернется предыдущее значение 2, поэтому сумма будет 4 (2+2)

alert(a);                    // 3
alert(b);                    // 3

